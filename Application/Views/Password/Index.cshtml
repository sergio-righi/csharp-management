@model Application.Models.Password.PasswordModel
@{
    ViewBag.Title = Title.Password;

    Layout = "~/Views/Shared/_Login.cshtml";
}

<div id="login_frame" class="login-frame">

    <div class="frame-header">

        <img class="brand" src="~/images/brand.png" />

    </div>

    <div class="frame-content">

        <div class="feedback-message">

            @await Html.PartialAsync("~/Views/Shared/Feedback/_Message.cshtml")

        </div>

        @using (Html.BeginForm("Index", "Password", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            @Html.HiddenFor(model => model.Token)

            <div class="row">

                @{
                    await Html.RenderPartialAsync("~/Views/Password/_Form.cshtml", Model);
                }

                <div class="col col-12">

                    <div class="form-group">

                        <button type="submit" class="button button-send button-plain button-stretch">@Button.Send</button>

                    </div>

                </div>

            </div>
        }

    </div>

</div>

@section ScriptsFooter
{
    <environment include="Development">
        <script type="text/javascript" src="~/js/vanilla/page/password/password.js"></script>
    </environment>
    <environment exclude="Development">
        <script type="text/javascript" src="~/js/vanilla/page/password/password.min.js"></script>
    </environment>
}