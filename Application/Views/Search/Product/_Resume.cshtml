@model ICollection<Domain.Entity.Specific.SProduct>

@if (Model.Count > 0)
{
    <ul class="list list-hover">

        @for (var i = 0; i < Model.Count; i++)
        {
            await Html.RenderPartialAsync("~/Views/Search/Product/_Item.cshtml", Model.ElementAt(i));
        }

    </ul>
}